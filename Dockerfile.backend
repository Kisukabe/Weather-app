# Backend Dockerfile: Python 3.10 + OpenJDK Java (PySpark) + FastAPI
FROM python:3.10-slim

# Cài đặt OpenJDK Java & các utility cơ bản cho PySpark
RUN apt-get update && apt-get install -y \
    default-jre-headless \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Thiết lập biến môi trường Java chuẩn cho Debian/Ubuntu
ENV JAVA_HOME=/usr/lib/jvm/default-java
ENV PATH="$JAVA_HOME/bin:$PATH"
ENV PYTHONPATH=/app

WORKDIR /app

# Sao chép và cài đặt dependencies cho Backend
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Sao chép toàn bộ mã nguồn
COPY . .

# Cài đặt package nội bộ
RUN pip install -e .

# Tạo thư mục lưu trữ artifacts và logs
RUN mkdir -p artifacts logs

# Mở cổng Backend REST API
EXPOSE 8000

# Health check cho Backend API
HEALTHCHECK CMD curl --fail http://localhost:8000/health || exit 1

# Khởi chạy FastAPI với Uvicorn server từ backend/app/main.py
CMD ["uvicorn", "backend.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
